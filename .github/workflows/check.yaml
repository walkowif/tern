---
name: Check 🛠

on:
  push:
    branches:
      - wasm-poc

jobs:
  build-wasm:
    name: Build WASM 🧑‍🏭
    needs: release
    uses: insightsengineering/r.pkg.template/.github/workflows/wasm.yaml@wasm-poc
