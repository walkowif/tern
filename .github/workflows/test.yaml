---
name: Debug

on:
  push:
    branches:
      - debug

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: |
          curl -vvv https://pharmaverse.slack.com

  links:
    if: github.event_name != 'push'
    name: Check URLs 🌐
    uses: insightsengineering/r.pkg.template/.github/workflows/links.yaml@main
